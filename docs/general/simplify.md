# /simplify

## What it is 

`/simplify` is a [built-in ("bundled") skill](https://code.claude.com/docs/en/skills#bundled-skills).
The official docs says:

`/simplify [focus]` - Review your recently changed files for code reuse, quality, and efficiency issues, then fix them. Spawns three review agents in parallel, aggregates their findings, and applies fixes. Pass text to focus on specific concerns: `/simplify focus on memory efficiency`.



## Notice

1. Cannot know what it really does, because it's part of the Claude Code binary, not a markdown file skill.

The description above says: Review your *recently changed* files. Not all files.
1. Seems like if we want to run it on an entire file, 
   we'd need to copy the file to a different folder that does not have a `.git` folder,
   and create a "claude project" there. 
   I did it, and indeed it found some issues and fixed them 
   (in Python code that had previously been generated by Claude Code).
2. I also checked: if I run it again after all the changes were made, whether it finds more.
   It decided to invoke the sequential-thinking (mcp server) even though I didn't ask it for that.
   And indeed it found more issues, some of them of exactly the type that it had previously found,
   so it's sloppy and needs to be invoked several times until no more changes.
3. Furthermore, it removes too many comments (comments that were written by Claude Code himself)
   because they violate its rule "don't allow a 'what' comment that simply describes what the code does".
   I think that in some cases the comment was good. 
   So if applying this tool, need to track what comments it removed and then put them back
4. In the third round it decided to: Verify behavior of hand-rolled functions against stdlib equivalents 
5. Furthermore, it keeps stopping and asking for trivial approvals.
   Perhaps if this would be run as a subprocess on claude + prompt, it would work? 
   - 50-50 chance, I'll check
   - I also tried: "Yes" + tab with addition: "and consider as if I say yes to all your further questions"